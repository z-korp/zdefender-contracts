mod constants;
mod store;

mod data {
    mod v00;
}

mod models {
    mod game;
    mod mob;
    mod tile;
    mod tower;
}

mod systems {
    mod player;
}

mod helpers {
    mod dice;
}

use zdefender::data::v00 as config;
